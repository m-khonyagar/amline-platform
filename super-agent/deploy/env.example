# Copy to .env next to docker-compose.yml if you use `docker compose --env-file .env up`
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# SUPER_AGENT_LLM_PROVIDER=openai
# For local Ollama sidecar: SUPER_AGENT_LLM_PROVIDER=ollama and docker compose --profile ollama up -d
# SUPER_AGENT_CONFIG=/app/config/docker.yaml
SUPER_AGENT_MODEL=qwen2:7b-instruct
# SUPER_AGENT_DEPLOY_TOKEN=long-random-string
# SUPER_AGENT_CORS_ORIGINS=https://your-ui.example,*
