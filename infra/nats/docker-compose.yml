# NATS — پیش‌نیاز Event-driven (فاز بعد). اجرا: docker compose up -d
services:
  nats:
    image: nats:2.10-alpine
    container_name: amline-nats
    ports:
      - "4222:4222"
      - "8222:8222"
    command: ["-js", "-m", "8222"]
