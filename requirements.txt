# Self-Improving AI Agent — dependencies
# Free-tier models: Ollama (local), HuggingFace Inference API, OpenRouter

# Core HTTP / async
httpx>=0.27.0,<1
aiohttp>=3.9.0,<4

# Pydantic for config / data models
pydantic>=2.5.0,<3
pydantic-settings>=2.2.0,<3

# LangGraph — workflow orchestration (primary framework)
langgraph>=0.2.0,<1
langchain-core>=0.3.0,<1
langchain-community>=0.3.0,<1

# AutoGen — multi-agent conversation framework
autogen-agentchat>=0.7.0,<1

# CrewAI — role-based team agent framework
crewai>=1.0.0,<2

# GitHub API integration
PyGithub>=2.3.0,<3

# YAML / JSON / config utilities
pyyaml>=6.0.1,<7

# Logging
structlog>=24.1.0,<25

# Testing
pytest>=8.0.0,<9
pytest-asyncio>=0.23.0,<1
