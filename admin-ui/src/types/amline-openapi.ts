/**
 * Re-exports from the committed OpenAPI snapshot. Regenerate from repo root:
 * `npm run openapi:refresh`
 *
 * Use at HTTP boundaries when you want compile-time alignment with `backend/backend`.
 */
export type { paths, components, operations } from '@amline/openapi-types'
